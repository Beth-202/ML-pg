**Chapter Introduction**  
When performing data analysis, it is necessary to perform preprocessing to understand and prepare the data. In this chapter, we will introduce the basic usage of a library commonly used in data analysis called Pandas.

Pandas is a useful library to process table data. In most real-world scenarios, data that you will find are not in ready-to-use format and require **data cleaning**.
In this notebook, we will look at Pandas' functionalities using an actual dataset that can be found online.

This is a library that will be used in later chapters as well, so let's make sure you have a good understanding of it here.
If you would like to learn Pandas more, please refer to the official documentation ([https://pandas.pydata.org/](https://pandas.pydata.org/)).

I want specific columns
        ↓
      df[...]

I want specific positions
        ↓
      .iloc

I want specific labels
        ↓
      .loc

I want rows satisfying conditions
        ↓
   Boolean masking

I want to sort
        ↓
   sort_values()

pd.read_csv()

pd.read_excel()

pd.read_json()

pd.read_parquet()
df[0:3]	rows 0–2
df.iloc[0:3]	rows 0–2
df.iloc[0, 3]	row 0, column 3
df.iloc[0:3, 0:3]	rows 0–2 AND columns 0–2
df['Name']	column Name
df[['Name', 'Score']]	columns Name and Score

RAW DATA
   ↓
inspect
   ↓
identify problems
   ↓
clean
   ↓
create features
   ↓
filter / investigate
   ↓
groupby / aggregate
   ↓
draw conclusions

DAY 2 — Ask the Dataset Questions 🔎
Theme: "Show me exactly the passengers I'm interested in."
Skills

column selection, [], .loc, .iloc, Boolean masking, &, |, ~, isin, between, query, sort_values

Now we're going to investigate the Titanic story.

Questions

1. What did the typical survivor look like?

Find all survivors and inspect their characteristics.

2. Which passengers were particularly vulnerable?

Find:

children
elderly passengers
third-class passengers

3. Did women survive more often than men?

Filter female survivors and male survivors.

4. Who were the wealthiest passengers?

Find the 10 passengers who paid the highest fares.

Then inspect their:

Name
Age
Sex
Pclass
Fare
Survived

5. Who were the youngest survivors?

Find the 10 youngest passengers who survived.

6. Find passengers who satisfy ALL of these:

Female + first class + survived + age between 20 and 40.

7. Find passengers who satisfy either:

Male AND first class
OR
Female AND third class.

8. Recreate three of your filters using .query().

🔥 Final question

Based on your filtering, what patterns are starting to emerge about survival?

Write 3–5 observations.

What you're learning

You're learning that Pandas isn't just "manipulating tables."

It's letting you interrogate data.
